// import { useNavigate, useParams } from 'react-router'
// // import { useDatabase } from '../slice/database-slice'
// import { useQuery } from '@tanstack/react-query'
// import { actionsProxy } from '@/lib/action-proxy'

export const useDBSchemas = () => {
  // const { connectionId } = useParams()
  // // const { connection } = useDatabase()
  // const navigate = useNavigate()

  // if (!connection) {
  //   navigate('/')
  //   return
  // }

  // return useQuery({
  //   queryKey: ['databaseSchemas', connectionId],
  //   queryFn: () => actionsProxy.getDatabaseSchemas.invoke({ connection })
  // })
}
